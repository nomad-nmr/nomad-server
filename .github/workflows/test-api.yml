name: Test API
on: push
jobs:
  test:
    env:
      JWT_SECRET: 'secret'
    runs-on: ubuntu-latest
    # environment: testing
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        working-directory: nomad-rest-api
        run: npm ci
      - name: Checking ENV
        run: echo "JWT_SECRET ${{env.JWT_SECRET}}"
      # - name: Test
      #   working-directory: nomad-rest-api
      #   run: npm run test-action
